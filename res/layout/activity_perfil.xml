<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="br.com.clean_up_mobile.activity.PerfilActivity" >

    <EditText
        android:id="@+id/editTextNome"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentRight="true"
        android:layout_below="@+id/imageViewPhotoPerfil"
        android:layout_marginTop="36dp"
        android:ems="10"
        android:enabled="false"
        android:inputType="textPersonName" >

        <requestFocus />
    </EditText>

    <Button
        android:id="@+id/buttonAlterarDados"
        style="?android:attr/buttonStyleSmall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentRight="true"
        android:layout_below="@+id/editTextNome"
        android:layout_marginTop="27dp"
        android:background="@layout/layout_button_azul"
        android:text="Alterar dados"
        android:textColor="@color/branco" />

    <ImageView
        android:id="@+id/imageViewPhotoPerfil"
        android:layout_width="200dp"
        android:layout_height="180dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:src="@drawable/diarista" />

    <ImageButton
        android:id="@+id/imageButtonEditarPhoto"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/editTextNome"
        android:layout_alignRight="@+id/imageViewPhotoPerfil"
        android:background="@android:color/transparent"
        android:src="@drawable/ic_editar" />

</RelativeLayout>
